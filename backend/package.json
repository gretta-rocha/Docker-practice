{
  "name": "docker",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "description": "This project provides a minimal API built with Node.js, containerized using a **multi-stage Docker build**. It exposes a single endpoint `/info` that returns:"
}
